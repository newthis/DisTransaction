<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:tx="http://www.springframework.org/schema/tx"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
	   http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx-2.5.xsd">

	<!--XA DataSource-->
	<bean id="xaDataSource" class="com.atomikos.jdbc.AtomikosDataSourceBean" init-method="init" destroy-method="close">
		<property name="uniqueResourceName" value="${dataSource.name}"/>
		<property name="xaDataSourceClassName" value="${dataSource.className}"/>
		<property name="xaProperties">
			<props>
				<prop key="URL">${dataSource.url}</prop>
				<prop key="user">${dataSource.username}</prop>
				<prop key="password">${dataSource.password}</prop>
			</props>
		</property>
		<property name="minPoolSize" value="${dataSource.minPoolSize}"/>
		<property name="maxPoolSize" value="${dataSource.maxPoolSize}"/>
		<property name="poolSize" value="${dataSource.poolSize}"/>
		<property name="maxLifetime" value="30000"/>
		<property name="testQuery" value="select 1 from dual"/>
	</bean>
	<!--XA TransactionManager-->
	<bean id="atomikosTransactionManager" class="com.atomikos.icatch.jta.UserTransactionManager" init-method="init" destroy-method="close">
		<property name="forceShutdown" value="${xa.transaction.forceShutDown}"/>
		<property name="transactionTimeout" value="${xa.transaction.timeout}"/>
	</bean>
	<!--User Transaction-->
	<bean id="atomikosUserTransaction" class="com.atomikos.icatch.jta.UserTransactionImp">
		<property name="transactionTimeout" value="${xa.transaction.timeout}"/>
	</bean>
	<!--Spring TransactionManager-->
	<bean id="tx" class="org.springframework.transaction.jta.JtaTransactionManager">
		<property name="transactionManager" ref="atomikosTransactionManager"/>
		<property name="userTransaction" ref="atomikosUserTransaction"/>
		<property name="allowCustomIsolationLevels" value="${xa.transaction.allowCustomIsolationLevels}"/>
	</bean>
	<!--JDBC Template-->
	<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="xaDataSource"/>
	</bean>
	<!--Annotation Driven-->
	<tx:annotation-driven transaction-manager="tx"/>
</beans>